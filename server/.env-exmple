MONGO_URI=mongodb://localhost:27017/Authentication_MVP
PORT = 3000
# Generate secret via (node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET={your secret}
NODE_ENV={development OR production}
CLIENT_URL={client URL}